# Entity Relationship Diagram — Digital Sari-Sari Store

This diagram reflects [`schema.sql`](./schema.sql) exactly: 10 tables normalized to **Third
Normal Form (3NF)**. Two many-to-many relationships are resolved through associative
("line item") tables:

- **Products ↔ Restocks** is resolved by `restock_items`.
- **Products ↔ Transactions** is resolved by `transaction_items`.

The "utang" (credit) ledger is a *running-balance* model: a charge is simply a
`transactions` row with `payment_type = 'credit'`, and `credit_payments` records money coming
back. Current balances are computed (not stored) by the `v_customer_balances` and
`v_utang_ledger` views.

> `schema.sql` is DDL-only; the ~676,767-row dataset (specific Filipino brands across six
> product groups) is generated by the Python pipeline and bulk-loaded. See [`README.md`](./README.md).

> PK = Primary Key · FK = Foreign Key · UK = Unique Key

```mermaid
erDiagram
    categories   ||--o{ products          : "classifies"
    units        ||--o{ products          : "measures"
    suppliers    ||--o{ restocks          : "delivers"
    restocks     ||--o{ restock_items     : "contains"
    products     ||--o{ restock_items     : "restocked as"
    customers    ||--o{ transactions      : "owes via (credit)"
    transactions ||--o{ transaction_items : "contains"
    products     ||--o{ transaction_items : "sold as"
    customers    ||--o{ credit_payments   : "repays"

    categories {
        int    category_id PK "Surrogate key"
        text   name        UK "e.g. Beverages, Snacks"
        text   description    "Optional notes"
        timestamptz created_at
    }

    units {
        int    unit_id PK "Surrogate key"
        text   code    UK "pc, sachet, bottle, kg, stick"
        text   name       "Human-readable unit"
        timestamptz created_at
    }

    suppliers {
        int    supplier_id PK "Surrogate key"
        text   name           "Wholesaler / dealer"
        text   contact_person
        text   phone
        text   address
        timestamptz created_at
    }

    products {
        int     product_id    PK "Surrogate key"
        int     category_id   FK "-> categories"
        int     unit_id       FK "-> units (selling unit)"
        text    name             "Catalog name"
        text    description
        text    barcode       UK "Nullable; tingi items unscanned"
        numeric current_price    "Today's selling price (>=0)"
        int     reorder_level    "Low-stock threshold"
        int     stock_on_hand    "Trigger-maintained running stock"
        boolean is_active         "Soft-delete flag"
        timestamptz created_at
        timestamptz updated_at    "Set by trigger"
    }

    customers {
        int     customer_id  PK "Surrogate key"
        text    full_name        "Legal/full name"
        text    nickname         "Palayaw — how owner files them"
        text    phone
        text    address
        numeric credit_limit     "Max allowed utang (>=0)"
        boolean is_active
        timestamptz created_at
        timestamptz updated_at    "Set by trigger"
    }

    restocks {
        int     restock_id   PK "Surrogate key"
        int     supplier_id  FK "-> suppliers"
        timestamptz restock_date  "When stock arrived"
        text    reference_no     "Supplier invoice / DR no."
        text    notes
        timestamptz created_at
    }

    restock_items {
        int     restock_item_id PK "Surrogate key"
        int     restock_id      FK "-> restocks (CASCADE)"
        int     product_id      FK "-> products (RESTRICT)"
        int     quantity           "Units received (>0)"
        numeric unit_cost          "Purchase cost snapshot (>=0)"
    }

    transactions {
        int     transaction_id   PK "Surrogate key"
        int     customer_id      FK "-> customers; NULL = cash walk-in"
        timestamptz transaction_date "When sold"
        text    payment_type        "'cash' | 'credit'"
        text    notes
        timestamptz created_at
    }

    transaction_items {
        int     transaction_item_id PK "Surrogate key"
        int     transaction_id      FK "-> transactions (CASCADE)"
        int     product_id          FK "-> products (RESTRICT)"
        int     quantity               "Units sold (>0)"
        numeric unit_price             "Selling price snapshot at sale (>=0)"
    }

    credit_payments {
        int     payment_id   PK "Surrogate key"
        int     customer_id  FK "-> customers (RESTRICT)"
        numeric amount          "Repayment amount (>0)"
        timestamptz payment_date "When paid"
        text    notes
        timestamptz created_at
    }
```

## Cardinality summary

| Relationship                                   | Type | Meaning                                                        |
|------------------------------------------------|------|---------------------------------------------------------------|
| categories → products                          | 1:N  | A category groups many products; a product has one category.  |
| units → products                               | 1:N  | A selling unit applies to many products.                      |
| suppliers → restocks                           | 1:N  | A supplier delivers many restock events.                      |
| restocks → restock_items                       | 1:N  | A restock has many lines (CASCADE on delete).                 |
| products → restock_items                       | 1:N  | A product appears on many restock lines.                      |
| customers → transactions                       | 1:N  | A customer has many (credit) sales; cash sales have no customer. |
| transactions → transaction_items               | 1:N  | A sale has many lines (CASCADE on delete).                    |
| products → transaction_items                   | 1:N  | A product appears on many sale lines.                         |
| customers → credit_payments                    | 1:N  | A customer makes many utang repayments.                       |

## Derived views (not tables)

| View                  | Purpose                                                                 |
|-----------------------|-------------------------------------------------------------------------|
| `v_product_stock`     | Per-product stock: strictly-derived `on_hand` next to trigger-maintained `maintained_on_hand` (reconciliation). |
| `v_customer_balances` | Running utang balance per customer: `SUM(credit sales) − SUM(payments)`. |
| `v_utang_ledger`      | Unified per-customer timeline of charges & payments with a running balance — the digital page of the old notebook. |
