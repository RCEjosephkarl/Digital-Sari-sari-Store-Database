# =============================================================================
#  Digital Sari-Sari Store — pinned dependencies (reproducible environment)
# =============================================================================
#  Python >= 3.10.  Create the environment with:
#
#      python3 -m venv .venv
#      ./.venv/bin/pip install -r requirements.txt
#      ./.venv/bin/pip install -e .          # install the `sarisari` package
#
#  ...or, much faster, with uv (https://docs.astral.sh/uv/):
#
#      uv venv .venv
#      uv pip install --python .venv/bin/python -r requirements.txt
#      uv pip install --python .venv/bin/python -e . --no-deps
#
#  (or simply: `make setup` — auto-detects uv and falls back to pip)
# -----------------------------------------------------------------------------

# --- Data engineering: synthesis, CSV, PostgreSQL load -----------------------
numpy==2.5.0
pandas==2.3.3
Faker==40.23.0
SQLAlchemy==2.0.51
psycopg[binary]==3.3.4
python-dotenv==1.2.2
pyarrow==24.0.0
tqdm==4.68.3

# --- Local zero-setup PostgreSQL (bundled server; no Docker/sudo needed) -----
pgserver==0.1.4

# --- Analytics & machine learning (the latter phase of the project) ----------
scikit-learn==1.9.0
matplotlib==3.11.0
seaborn==0.13.2
jupyterlab==4.6.0
ipykernel==7.3.0
nbformat==5.10.4

# --- Reporting & data visualization (notebook + Streamlit dashboard) ---------
streamlit==1.50.0
plotly==6.3.0
